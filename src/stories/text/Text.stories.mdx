import {Canvas, Meta, Story} from "@storybook/addon-docs/blocks";
import Template from "./TextTemplate";
import Text from 'components/Text'

<Meta title="Text/Text" component={Text}/>

### Default
<Canvas>
    <Story name="default" args={{children: "text"}}>
        {Template.bind({})}
    </Story>
</Canvas>

### Positive
<Canvas>
  <Story name="positive" args={{children: "text", mode: "positive"}}>
    {Template.bind({})}
  </Story>
</Canvas>

### Negative
<Canvas>
  <Story name="negative" args={{children: "text", mode: "negative"}}>
    {Template.bind({})}
  </Story>
</Canvas>
