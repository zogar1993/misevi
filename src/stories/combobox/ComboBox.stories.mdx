import {Canvas, Meta, Story} from "@storybook/addon-docs/blocks";
import {Template, ComboboxStoryTypes, items, long_items} from "./ComboBoxTemplate";

<Meta title="Form/Combobox" component={ComboboxStoryTypes}/>

### Default
<Canvas>
  <Story name="with value" args={{label: "with value", value: "apostle", options: items}}>
    {Template.bind({})}
  </Story>
</Canvas>

### Invalid Value
<Canvas>
  <Story name="with invalid value" args={{label: "with invalid value", value: "spostore", options: items}}>
    {Template.bind({})}
  </Story>
</Canvas>

### Long Dropdown
<Canvas>
  <Story name="long dropdown" args={{label: "long dropdown", value: null, options: long_items}}>
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled
<Canvas>
  <Story name="disabled with selected" args={{label: "disabled with selected", value: "apostle", options: items, disabled: true}}>
    {Template.bind({})}
  </Story>
  <Story name="disabled empty" args={{label: "disabled empty", value: null, options: items, disabled: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

### Skeleton
<Canvas>
  <Story name="no value and no options" args={{label: "no value and no options"}}>
    {Template.bind({})}
  </Story>
  <Story name="no value" args={{label: "no value", options: items}}>
    {Template.bind({})}
  </Story>
  <Story name="no options" args={{label: "no options", value: "irrelevant"}}>
    {Template.bind({})}
  </Story>
</Canvas>

