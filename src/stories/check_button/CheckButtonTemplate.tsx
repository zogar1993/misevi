import CheckButton, { CheckButtonProps } from 'components/CheckButton'
import React from 'react'

export default function (args: CheckButtonProps) {
  return <CheckButton {...args} onChange={() => {}} />
}
