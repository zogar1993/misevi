import {Canvas, Meta, Story} from '@storybook/addon-docs/blocks';
import Button from 'components/Button';
import Template from "./ButtonTemplate";

<Meta title="Form/Button" component={Button}/>

<Canvas>
    <Story name="size small" args={{size: "small", children: "small"}}>
        {Template.bind({})}
    </Story>
    <Story name="size medium" args={{size: "medium", children: "medium"}}>
        {Template.bind({})}
    </Story>
    <Story name="size large" args={{size: "large", children: "large"}}>
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="disabled" args={{disabled: true, children: "disabled"}}>
        {Template.bind({})}
    </Story>
</Canvas>
